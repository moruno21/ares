import DefaultSpinner from '@ares/ui/components/Spinner'
import styled from 'styled-components'

export const Container = styled.div`
  bottom: 0;
  display: flex;
  left: 0;
  position: fixed;
  right: 0;
  top: 0;
`

export const Spinner = styled(DefaultSpinner)`
  margin: auto;
`
