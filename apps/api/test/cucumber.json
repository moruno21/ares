{
  "default": {
    "paths": ["../../features/**/*.feature"],
    "publishQuiet": true,
    "require": ["./test/steps/**/*.ts"],
    "requireModule": ["tsconfig-paths/register", "ts-node/register"]
  }
}
